# Neynar API Key (for feed, user data, etc.)
# Used by apps/web for the API server
NEYNAR_API_KEY=your_neynar_api_key_here

# Mobile-specific: API URL (used by the mobile app to connect to the web API)
# Leave empty for production (defaults to https://litecast.xyz)
# Set to http://localhost:3000 for local development
EXPO_PUBLIC_API_URL=

# Legacy: Neynar API key exposed to mobile (for gradual migration)
EXPO_PUBLIC_NEYNAR_API_KEY=your_neynar_api_key_here

# Farcaster App Credentials (for signer creation)
# These are the credentials for YOUR app's Farcaster account
# The app needs a Farcaster FID and the seed phrase of the custody address
FARCASTER_APP_FID=your_app_fid_here
FARCASTER_APP_MNEMONIC=your twelve word seed phrase goes here for signing
