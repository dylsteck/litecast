# Litecast

- Official Farcaster API only (`lib/farcaster`). No Neynar. No debug console noise.
- React Query for all server state. No fetch-in-useEffect.
- Litecast visuals: black / iOS gray, no purple, no card backgrounds, systemMaterial blur.
- Keep the client small. Do not import the Warpcast monorepo.
